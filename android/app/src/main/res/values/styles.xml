<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Status bar color - transparent for light theme -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Status bar text color - dark content for light theme -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- Set the primary color to avoid Material default purple -->
        <item name="colorPrimary">@color/button_background_color</item>
        <item name="colorPrimaryVariant">@color/button_alternative_text_color</item>
        <item name="colorOnPrimary">@color/button_text_color</item>
        <item name="bottomSheetDialogTheme">@style/AppTheme.BottomSheetDialog</item>
    </style>

    <style name="AppTheme.BottomSheetDialog" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppTheme.BottomSheet</item>
    </style>

    <style name="AppTheme.BottomSheet" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="shapeAppearance">@null</item>
        <item name="shapeAppearanceOverlay">@null</item>
        <item name="backgroundTint">@null</item>
    </style>
</resources>